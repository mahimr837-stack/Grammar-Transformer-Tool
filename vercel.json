{
  "buildCommand": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/english-grammar run build && cp -r artifacts/english-grammar/dist ./dist-vercel",
  "outputDirectory": "dist-vercel",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
